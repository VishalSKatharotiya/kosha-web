/**
 * Constants Index
 * Centralized export for all application constants
 * 
 * Usage:
 * import { ROUTES, CONTACT_INFO, COMPANY_INFO } from '@/constants';
 * or
 * import { ROUTES } from '@/constants/routes';
 */

// Export all route constants
export * from './routes';

// Export all config constants
export * from './config';

// Default exports
export { default as ROUTES } from './routes';
export { default as CONFIG } from './config';

 * Constants Index
 * Centralized export for all application constants
 * 
 * Usage:
 * import { ROUTES, CONTACT_INFO, COMPANY_INFO } from '@/constants';
 * or
 * import { ROUTES } from '@/constants/routes';
 */

// Export all route constants
export * from './routes';

// Export all config constants
export * from './config';

// Default exports
export { default as ROUTES } from './routes';
export { default as CONFIG } from './config';

